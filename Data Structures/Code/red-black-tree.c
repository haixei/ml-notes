#include <stdio.h>

// Basic node structure
struct Node{
    int data, color;
    struct Node *left, *right, *parent;
};

int main(){
    struct Node *root = NULL;
    // Test the code
    return 0;
}
